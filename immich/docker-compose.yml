name: immich

services:
  immich-server:
    container_name: immich_server
    image: ghcr.io/immich-app/immich-server:${IMMICH_VERSION:-release}
    extends:
      file: hwaccel.transcoding.yml
      service: quicksync
    volumes:
      - ${UPLOAD_LOCATION}:/data
      - ${THUMB_LOCATION}:/data/thumbs
      - ${ENCODED_VIDEO_LOCATION}:/data/encoded-video
      - ${PROFILE_LOCATION}:/data/profile
      - ${BACKUP_LOCATION}:/data/backups
      - /etc/localtime:/etc/localtime:ro
      - /media/daniel/google-photos:/mnt/google-photos
    env_file:
      - .env
    environment:
      - TZ=${TZ}
    labels:
      - "pangolin.proxy-resources.immich.name=Immich Photos"
      - "pangolin.proxy-resources.immich.protocol=http"
      - "pangolin.proxy-resources.immich.full-domain=photos.${DOMAIN}"
      - "pangolin.proxy-resources.immich.targets[0].enabled=true"
      - "pangolin.proxy-resources.immich.targets[0].method=http"
    networks:
      - proxy
      - immich
    depends_on:
      - redis
      - database
    restart: always
    healthcheck:
      disable: false

  immich-machine-learning:
    container_name: immich_machine_learning
    # For hardware acceleration, add one of -[armnn, cuda, rocm, openvino, rknn] to the image tag.
    # Example tag: ${IMMICH_VERSION:-release}-cuda
    image: ghcr.io/immich-app/immich-machine-learning:${IMMICH_VERSION:-release}-openvino
    extends:
      file: hwaccel.ml.yml
      service: openvino
    volumes:
      - model-cache:/cache
    env_file:
      - .env
    environment:
      - TZ=${TZ}
    networks:
      - immich
    restart: always
    healthcheck:
      disable: false

  redis:
    container_name: immich_redis
    image: docker.io/valkey/valkey:8-bookworm@sha256:fea8b3e67b15729d4bb70589eb03367bab9ad1ee89c876f54327fc7c6e618571
    environment:
      - TZ=${TZ}
    networks:
      - immich
    healthcheck:
      test: redis-cli ping || exit 1
    restart: always

  database:
    container_name: immich_postgres
    image: ghcr.io/immich-app/postgres:14-vectorchord0.4.3-pgvectors0.2.0@sha256:41eacbe83eca995561fe43814fd4891e16e39632806253848efaf04d3c8a8b84
    environment:
      - POSTGRES_PASSWORD: ${DB_PASSWORD}
      - POSTGRES_USER: ${DB_USERNAME}
      - POSTGRES_DB: ${DB_DATABASE_NAME}
      - POSTGRES_INITDB_ARGS: '--data-checksums'
      - TZ=${TZ}
      # Uncomment the DB_STORAGE_TYPE: 'HDD' var if your database isn't stored on SSDs
      # DB_STORAGE_TYPE: 'HDD'
    volumes:
      # Do not edit the next line. If you want to change the database storage location on your system, edit the value of DB_DATA_LOCATION in the .env file
      - ${DB_DATA_LOCATION}:/var/lib/postgresql/data
    networks:
      - immich
    shm_size: 128mb
    restart: always

volumes:
  model-cache:

networks:
  proxy:
    external: true
  immich:
