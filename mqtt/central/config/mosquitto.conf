# Central aggregator — receives opted-in sites' subtrees via per-site bridges.
listener 1883
allow_anonymous false
password_file /run/mosquitto/passwd
acl_file /run/mosquitto/acl

persistence true
persistence_location /mosquitto/data/
