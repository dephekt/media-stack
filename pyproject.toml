[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = [
    "**/secrets/",
    "keycloak-import/",
    "keycloak-export/",
    ".venv/",
]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "double"
